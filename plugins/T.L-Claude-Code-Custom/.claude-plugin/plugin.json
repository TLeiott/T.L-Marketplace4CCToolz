{
  "name": "T.L-Claude-Code-Custom",
  "version": "2.2.3",
  "description": "Launch Claude Code with custom OpenRouter models via two modes: claude-custom-proxy (shared daemon) and claude-custom (direct API). Shared config via claude-custom.json. Skills: /init-claude-code-custom, /proxy-up, /proxy-down, /cleanup.",
  "author": {
    "name": "TLeiott"
  }
}
